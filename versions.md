# Versions

## 2026-03-26-web.1
- Добавлен локальный веб-интерфейс (`web/index.html`, `web/styles.css`, `web/app.js`).
- Добавлен отдельный файл версии интерфейса (`web/version.js`).
- Добавлен локальный HTTP-сервер `webui_server.py` для запуска экспорта, сбора логов и просмотра статуса задач.
- Добавлен запуск веб-сервера в 1 клик: `run_web.command`.
- В UI есть статус задачи, поток логов и путь к итоговому файлу.

## 2026-03-26-web.2
- Добавлен корневой `index.html` для прямого запуска интерфейса (двойной клик по файлу).
- Корневой `index.html` работает с API сервера `http://127.0.0.1:8765`.
- В `webui_server.py` добавлены CORS-заголовки и обработка `OPTIONS`.

## 2026-03-26-web.3
- Добавлен единый запуск [start_console.command](/Users/macbook/Documents/История заказа/start_console.command): поднимает backend и открывает UI автоматически.

## 2026-03-26-web.4
- Исправлен выбор runtime-шаблона `SaleOrder.List`: теперь runtime-подмена берется только для payload с `ProductStateId=Done`.
- Убрана полная подмена sale-фильтра runtime-пакетом: фильтр `Фильтр` стабильно берется из HAR-шаблона.
- Из runtime теперь применяются только технические параметры запроса (`method` и `x-calledmethod`) для совместимости версии API.

## 2026-03-26-web.5
- Добавлен гибридный режим фильтра: если `SaleOrder.List` с `ProductStateId=Done` не пойман, переносится runtime-контекст фильтров (`CRMFilter`, `Company`, `OurOrgFilter`, `Sales*`, `SaleNomenclatureFilter` и др.) из ближайшего `SaleOrder.List`.
- Базовые условия выгрузки (дата, `ProductStateId=Done`) по-прежнему задаются скриптом.
- Добавлены диагностические логи: найден ли runtime `Done`-запрос и сколько полей фильтра было перенесено.
- Обновлена UI-метка версии в `web/version.js` до `2026.03.26-web.5`.
- Для статики веб-сервера добавлены anti-cache заголовки (`Cache-Control: no-store` и др.), чтобы версия и фронтенд не залипали в кеше.

## 2026-03-26-web.6
- Добавлен авто-подбор `Reglament`: скрипт считывает счетчик `Выполнен/Отмечено` со страницы и пробует варианты фильтра, выбирая ближайший по количеству заказов.
- Добавлена функция чтения UI-счетчика из DOM и диагностический вывод выбранного варианта.
- Добавлена поддержка `reglament_override` при сборке payload и в загрузке реестра.

## 2026-03-26-web.7
- В веб-интерфейс добавлены кнопки `Копировать` и `Скрыть лог`.
- Исправлена очистка лога во время активного выполнения: очищенные строки не подгружаются повторно.
- Для консоли включен перенос длинных строк (`pre-wrap`, `overflow-wrap:anywhere`), чтобы блок лога не раздувал страницу по ширине.

## 2026-03-26-web.8
- Расширен автоподбор выборки заказов: теперь подбирается не только `Reglament`, но и уровень ослабления контекстных фильтров (`context_relax_level`).
- Добавлены режимы ослабления контекста (`CRMFilter`, затем дополнительно `SaleNomenclatureFilter`/`NotFields`/`ProductStateFields`) для приближения к счетчику UI.
- Изменен приоритет выбора варианта: минимальная разница с UI, при равенстве — вариант не меньше UI, затем минимальный `context_relax_level`.

## 2026-03-26-web.9
- Добавлен пост-этап уточнения выборки к UI-счетчику: если серверный фильтр сильно расходится с UI, скрипт подбирает лучший срез заказов по полям `Company`, `RealCompany`, `OriginCompany`, `Source`, `Type`, `Warehouse`.
- При наличии вариантов выше UI сначала берется ближайший “сверху”, затем выполняется уточняющий срез.
- Добавлен диагностический лог применения уточняющего среза (`Применен доп.срез к UI-счетчику: ...`).

## 2026-03-26-web.10
- Улучшен этап уточнения к UI-счетчику: добавлен подбор не только по одному полю, но и по комбинациям из двух полей (`Company`, `RealCompany`, `OriginCompany`, `Source`, `Type`, `Warehouse`).
- Это позволяет находить более точный срез (например `Source+Company`) и сильнее приближаться к счетчику UI.

## 2026-03-26-web.11
- Добавлен retry для временных ошибок API (`500/502/503/504`) в RPC-вызовах, чтобы подбор не падал из-за кратковременных сбоев dispatcher.
- В цикле подбора фильтров ошибки отдельных вариантов теперь логируются и пропускаются, а не роняют весь процесс.

## 2026-03-26-web.12
- Добавлен этап subset-sum по бакетам одного поля: скрипт может выбирать не одно значение (например `Source=26`), а комбинацию нескольких значений поля для более точного приближения к UI-счетчику.
- Приоритет уточнения теперь включает: один бакет, комбинации двух полей и комбинации нескольких значений одного поля.

## 2026-03-26-web.13
- Убран принудительный сброс контекстных полей фильтра (`Company`, `OurOrgFilter`, `Sales`, `SalesPoints`, `Workplaces`, `Warehouses` и др.) в `build_sale_payload`.
- Это предотвращает расширение выборки в сторону заказов `Зала` и сохраняет контекст delivery из runtime/HAR.

## 2026-03-26-web.14
- В Excel (`Реестр`) добавлены диагностические поля: `Reglament`, `Source`, `Type`, `Address`, `WarehouseName`.
- Это позволяет сразу визуально отличать заказы доставки от заказов зала в выгрузке.

## 2026-03-26-web.15
- Убрано копирование `SaleNomenclatureFilter` из runtime-контекста в шаблон, т.к. это вызывало RPC-ошибку типа (`AccountingDelegation` bool parse error).
- В `wait_until_service_ready` добавлен fail-fast для ошибок формата фильтра (больше не маскируется как проблема авторизации).
- Добавлена защита от падения на `page.bring_to_front()` при закрытой странице/контексте.

## 2026-03-26-web.16
- Добавлена кнопка `Остановить` в веб-интерфейс.
- Реализован API `POST /api/stop` для остановки активной задачи экспорта.
- Сервер теперь хранит PID активного процесса, помечает `stop_requested` и выставляет статус задачи `stopped`.
- В UI добавена обработка статуса `STOPPED` и вывод сообщения об остановке пользователем.

## 2026-03-26-web.17
- Перед runtime-capture добавлен переход на `page/delivery` и клик по вкладке `Выполнен`.
- Это повышает вероятность захвата правильного `SaleOrder.List` именно из delivery-контекста.

## 2026-03-26-web.18
- Добавлен строгий режим состава доставки: если не пойман runtime `SaleOrder.List` с `ProductStateId=Done`, выгрузка останавливается с инструкцией (по умолчанию без эвристического подбора).
- Эвристический fallback оставлен только по явному флагу `--allow-heuristic-fallback`.
- При наличии runtime Done-запроса загрузка идет напрямую по его delivery-контексту без подбора по `Company`.

## 2026-03-26-web.19
- Добавлен безопасный автоподбор `scope_relax_mode` при расхождении с UI: `keep`, `no_ourorg`, `no_company`, `no_org_scope`.
- Подбор меняет только организационный охват (`OurOrgFilter`/`Company`) и не включает эвристики по залу.

## 2026-03-26-web.20
- В `webui_server.py` добавлен авто-ретрай веб-задачи: если строгий запуск падает из-за `runtime Done not captured`, сервер автоматически повторяет запуск с `--allow-heuristic-fallback`.
- Это убирает падение задачи в Web UI и сохраняет непрерывность работы.

## 2026-03-26-web.21
- Усилен детект `strict runtime miss` в `webui_server.py`: fallback включается по нескольким сигнатурам traceback, а не по одной точной строке.
- Добавлена дополнительная проверка последних логов задачи перед решением о retry с `--allow-heuristic-fallback`.

## 2026-03-26-web.22
- Изменен retry в `webui_server.py`: при любой ошибке первого запуска сервер гарантированно делает второй запуск с `--allow-heuristic-fallback`.
- Это устраняет пропуски авто-fallback из-за нестабильного формата ошибок в логе/traceback.

## 2026-03-26-web.23
- В `export_delivery_statuses.py` убрано аварийное завершение при `runtime Done not captured`.
- Теперь exporter сам автоматически включает эвристический fallback и продолжает выполнение с предупреждением.
- Это делает поведение устойчивым даже если web-сервер не успел обновиться.

## 2026-03-26-web.24
- Исправлен запуск через `start_console.command`: теперь backend на `8765` всегда перезапускается, чтобы не оставался старый процесс без `POST /api/stop`.
- В UI (`web/app.js`) добавлена явная подсказка при `HTTP 404` на остановке: предложен перезапуск backend.

## 2026-03-26-web.25
- Возвращен строгий режим в `export_delivery_statuses.py`: если runtime `SaleOrder.List` с `ProductStateId=Done` не пойман, экспорт завершается ошибкой (без авто-включения эвристики).
- В `webui_server.py` отключен автоматический второй запуск с `--allow-heuristic-fallback`; web-задача теперь не подмешивает эвристический (зальный) состав.
- Итог: в вебе либо корректный delivery-состав, либо явная ошибка с инструкцией открыть `Доставка -> Выполнен` и повторить запуск.

## 2026-03-26-web.26
- Для `--har` добавлен режим `auto` (по умолчанию).
- Автовыбор HAR теперь использует приоритет: `/Users/macbook/Downloads/rest.saby.ru_dost.har`, затем `/Users/macbook/Downloads/rest.saby.ru history.har`.
- Это позволяет веб-запуску автоматически брать специализированный HAR доставки без ручной подстановки пути.

## 2026-03-26-web.27
- Если в активном HAR (например `rest.saby.ru_dost.har`) нет шаблона `История.History_Of_Instance`, exporter автоматически подгружает history-шаблон из резервного HAR.
- При успешном fallback в лог пишется источник: `History-шаблон взят из резервного HAR: ...`.
- Это устраняет падение `Ошибка чтения HAR: Не найден подходящий шаблон История.History_Of_Instance в HAR` при использовании HAR доставки.

## 2026-03-27-web.28
- Усилен выбор шаблона `SaleOrder.List` из HAR: запросы с `referer=/page/delivery` теперь имеют приоритет над остальными.
- Добавлен признак delivery-контекста HAR (`har_sale_delivery_context`) в `TemplateBundle`.
- В строгом режиме при runtime-miss `ProductStateId=Done` и наличии delivery-контекста в HAR запуск продолжается в строгом HAR-режиме (без эвристики).
- Убрано опасное подмешивание фильтров из "ближайшего" runtime `SaleOrder.List`, когда runtime `Done` не пойман, чтобы не уехать в заказы зала.

## 2026-03-27-web.29
- В строгом режиме добавлен probe по `Reglament` перед расширением scope: проверяются варианты `Reglament=<template>`, `Reglament=1`, `Reglament=2` и `Reglament=<unset>`.
- Добавлена поддержка явного снятия поля `Reglament` в payload (`clear_reglament=True`) без включения эвристики.
- Это помогает выйти из фиксированного сценария `49 заказов`, если ограничение сидит именно в `Reglament`.

## 2026-03-27-web.30
- В строгом delivery-режиме добавлен probe по `context_relax_level` до `scope_relax_mode`.
- Логируются варианты `context_relax_level=0/1/2` и автоматически выбирается ближайший к UI-счетчику.
- Это позволяет снять внутренние ограничения payload (в первую очередь `CRMFilter`) без перехода в эвристику и без подмешивания зала.

## 2026-03-27-web.31
- Ослабляющие режимы (`scope_relax_mode`, `context_relax_level`, `clear_reglament`) теперь удаляют поля из record-схемы фильтра (`s/d`), а не только ставят `null`.
- Это важно для Saby API: часть ограничителей игнорирует `null`, но корректно снимается при полном удалении поля.
- Цель: чтобы варианты relax реально меняли выдачу и выводили процесс из фиксированного `49`.

## 2026-03-27-web.32
- В строгом delivery-режиме исключен вариант `Reglament=1` из probe, т.к. это маркер заказов зала.
- Подбор `Reglament` в strict теперь ограничен безопасными delivery-вариантами: `Reglament=2` и `Reglament=<unset>`.

## 2026-03-27-web.33
- Усилен runtime-capture `SaleOrder.List`: перехват сети переведен с `page.on("request")` на `browser_context.on("request")`.
- Это позволяет ловить запросы, уходящие не только из текущей страницы/фрейма, и повышает шанс поймать живой `ProductStateId=Done`.

## 2026-03-27-web.34
- Исправлена пагинация в `fetch_done_orders` и `fetch_order_status_history`: теперь используется `Навигация.HasMore` и серверный `Навигация.Position` из ответа, а не только эвристика `len(rows) < limit`.
- Это синхронизирует поведение с UI-скроллом (infinite scroll), где следующая порция может быть меньше `limit`, но список еще не закончился.
- Добавлена защита от зацикливания по сигнатуре позиции.

## 2026-03-27-web.35
- В веб-интерфейс добавлена кнопка `Скачать Excel` (активируется после успешной выгрузки).
- В `webui_server.py` добавлен endpoint `GET /api/download?job_id=...`, который отдает итоговый `.xlsx` как вложение.
- Добавлена валидация пути выгрузки: скачивание разрешено только из каталога `exports`.

## 2026-03-27-web.36
- В Excel добавлен лист `Аналитика` по каждому заказу: ресторан, курьер, оператор (если есть), время начала/завершения, общее время и длительности этапов (`Обработка`, `Готовка`, `Сборка`, `Доставка`), а также bottleneck по заказу.
- Добавлен лист `Узкие места`:
  - сводка по этапам (count/avg/median/p90/max),
  - сводка по ресторанам (число заказов, среднее общее, среднее доставки, p90 общего).
- Расчеты строятся из истории смен статусов и данных реестра заказов.

## 2026-03-27-web.37
- Добавлен лист `Проблемные заказы`:
  - top-20 заказов по `DeliveryMin`,
  - top-20 заказов по `TotalMin`.
- Для каждого проблемного заказа выводятся ресторан, курьер, оператор, длительности этапов и bottleneck.

## 2026-03-27-web.38
- В строгом HAR-режиме (когда runtime `SaleOrder.List` с `ProductStateId=Done` не пойман) отключен UI-автоподбор фильтров.
- Это убирает лишние повторные проходы по реестру (`Reglament/context_relax/scope_relax`) и заметно ускоряет запуск на датах, где UI-счетчик относится к другому контексту/дате.

## 2026-03-27-web.39
- Добавлен визуальный блок `Аналитика доставки` в веб-интерфейс:
  - KPI по доставке (среднее, P90, число опозданий),
  - график этапов (`Обработка`, `Готовка`, `Сборка`, `Доставка`, `Итого`),
  - распределение узких мест по статусам,
  - таблица ресторанов с риском опозданий,
  - таблица проблемных заказов.
- В `webui_server.py` добавлен endpoint `GET /api/analytics?job_id=...`, который читает лист `Аналитика` из итогового Excel и отдает JSON для фронта.

## 2026-03-27-web.40
- В `export_delivery_statuses.py` UI-счетчик (`Выполнен/Отмечено`) отключен по умолчанию, чтобы не запускать лишние циклы автоподбора количества заказов на разных датах.
- Добавлен явный флаг `--align-to-ui-count` для ручного включения старого поведения подгонки к UI.

## 2026-03-27-web.41
- Дашборд в вебе дополнен метриками из вкладки `Статусы`:
  - фактический поток этапов (`Не распределен->Выполнение`, `Выполнение->Сборка`, `Сборка->Доставка`, `Доставка->Выполнен`) с avg/P90;
  - топ переходов статусов по количеству;
  - KPI по доле заказов без этапа `Доставка`.
- В `webui_server.py` расширен `GET /api/analytics`: теперь возвращает блок `status_flow`, рассчитанный из листа `Статусы`.

## 2026-03-27-web.42
- После обновления страницы UI автоматически восстанавливает последнюю задачу через `GET /api/latest`.
- Если последняя задача успешна, сразу показываются:
  - путь к файлу,
  - активная кнопка `Скачать Excel`,
  - дашборд аналитики без повторного запуска выгрузки.
- Если последняя задача все еще выполняется, UI автоматически подцепляется к ней и продолжает онлайн-лог.

## 2026-03-27-web.43
- Исправлено обновление дашборда по выбранной дате:
  - `GET /api/analytics` и `GET /api/download` теперь принимают `date=YYYY-MM-DD` и работают напрямую с `exports/order_status_history_<date>.xlsx`.
  - Кнопка `Обновить` в аналитике и смена даты в поле теперь загружают дашборд именно для выбранной даты, а не только для последней задачи.
  - Кнопка `Скачать Excel` скачивает файл выбранной даты (если аналитика загружена по дате).

## 2026-03-27-web.44
- Пересчитана аналитика этапов по правилам статусов:
  - `Не распределен -> Выполнение` = обработка,
  - `Выполнение -> Сборка/Доставка` = готовка,
  - `Сборка -> Доставка/Самовывоз` = сборка,
  - `Доставка -> Выполнен` = доставка,
  - для `Самовывоз` этап доставки не считается (используется выдача `Самовывоз -> Выполнен`).
- В дашборд добавлен блок `Нагрузка по времени` (почасовой старт заказов + число просрочек).
- Добавлена полная таблица всех заказов за день с рестораном, курьером/оператором, этапами в минутах и причиной задержки.
- Просрочки (`>60` минут) теперь выделяются в таблицах для наглядности.
- Выбранная дата сохраняется в браузере, и после перезагрузки UI сначала пытается восстановить дашборд именно для этой даты.

## 2026-03-27-web.45
- Из дашборда удалены блоки:
  - `Фактический поток статусов (Статусы)`,
  - `Топ переходов статусов`.
- Эти секции дублировали этапную аналитику выше, поэтому оставлены только прикладные блоки для контроля нагрузки, опозданий и проблемных заказов.

## 2026-03-27-web.46
- Добавлен фильтр по ресторану в веб-интерфейсе:
  - новый endpoint `GET /api/restaurants?date=YYYY-MM-DD`,
  - `GET /api/analytics` теперь принимает `restaurant` и считает дашборд по выбранному ресторану.
- Добавлена кнопка `Скачать PDF` и endpoint `GET /api/report_pdf?date=...&restaurant=...`.
- PDF-отчет формируется как таблица для быстрого анализа проблем:
  - ресторан, номер заказа,
  - план прибытия (`NextDateWTZText`),
  - фактическое завершение,
  - отклонение план/факт (мин),
  - длительности этапов (обработка/готовка/сборка/доставка или выдача),
  - причина задержки.
- В PDF и в таблицах дашборда просроченные и SLA-проблемные строки выделяются визуально.

## 2026-03-27-web.47
- Исправлен расчет для заказов "ко времени" (предзаказов, принятых заранее):
  - этап `Готовка` (`Выполнение -> Сборка/Доставка`) теперь считается не ранее, чем от `плановое время - 60 минут`,
  - итоговое время заказа для SLA также считается от `T-60`, чтобы не включать ожидание с прошлого дня.
- Это устраняет ложные аномалии вида `готовка 1000+ минут` для предзаказов.

## 2026-03-27-web.48
- Проведена проверка этапа `Доставка` для заказов "ко времени": в текущих выгрузках аномалии предзаказного накопления не выявлены.
- Добавлена защитная логика на будущее: `Доставка -> Выполнен` также ограничивается якорем `T-60`, чтобы ожидание до целевого окна не раздувало этап.

## 2026-03-28-web.49
- В таблицу `Все заказы за день` добавлены колонки:
  - `К какому времени`,
  - `Δ план/факт, мин`.
- В таблицу `Проблемные заказы` добавлены:
  - `К какому времени`,
  - `Δ план/факт`.
- Положительное отклонение (факт позже плана) подсвечивается для быстрого визуального контроля SLA.

## 2026-03-28-web.50
- Исправлен кейс самовывоза с поздним закрытием:
  - для заказов типа `Самовывоз` SLA-`Итого` теперь считается до перехода в `Самовывоз` (готов к выдаче), а не до `Выполнен`.
  - это убирает ложные `300-600+ мин` в общем времени заказа из-за закрытия в конце смены.
- Для самовывоза очищена диагностика причин: не выводятся ложные `нет этапа Обработка/Готовка/Сборка`.

## 2026-03-28-web.51
- Добавлена автоматическая сборка Windows-дистрибутива через GitHub Actions:
  - workflow `.github/workflows/build-windows-exe.yml`,
  - сборка `saby_export_console.exe` и `export_delivery_statuses.exe`,
  - публикация zip-артефакта `saby_export_console_windows`.
- Добавлена поддержка запуска worker-экспорта из веб-консоли в exe-режиме:
  - `webui_server.py` теперь ищет рядом `export_delivery_statuses.exe` и запускает его напрямую,
  - если worker `.exe` не найден, используется прежний python-режим (`python export_delivery_statuses.py`).

## 2026-03-28-web.52
- Исправлен мгновенный выход `saby_export_console.exe` на Windows:
  - в `main()` проверка наличия экспортера теперь учитывает `export_delivery_statuses.exe`, а не только `export_delivery_statuses.py`.
- Добавлен Windows-лаунчер `start_windows_console.bat`:
  - запускает консоль из текущей папки,
  - пишет лог в `console.log`,
  - не закрывает окно сразу при ошибке (`pause`), чтобы видеть причину.
- В CI-сборку (`build-windows-exe.yml`) добавлено включение `start_windows_console.bat` в дистрибутив.

## 2026-03-28-web.53
- Исправлен падение Windows-сборки на старте (`ModuleNotFoundError: reportlab`).
- `reportlab` добавлен в `requirements.txt`, чтобы GitHub Actions устанавливала библиотеку перед сборкой `saby_export_console.exe`.

## 2026-03-28-web.54
- Улучшен UX Windows-запуска:
  - `start_windows_console.bat` теперь автоматически открывает браузер на `http://127.0.0.1:8765`,
  - консоль продолжает работать как сервер, лог пишется в `console.log`.

## 2026-03-28-web.55
- Исправлена ошибка Windows worker-EXE `ModuleNotFoundError: No module named 'greenlet._greenlet'`.
- В зависимости добавлен `greenlet` (`requirements.txt`).
- В CI-сборке PyInstaller для `export_delivery_statuses.exe` добавлены:
  - `--collect-all greenlet`
  - `--collect-all playwright`
  чтобы гарантированно включать бинарные модули и runtime-компоненты Playwright.

## 2026-03-28-web.56
- Усилен фикс упаковки `greenlet` для Windows worker-EXE:
  - добавлены `--hidden-import greenlet` и `--hidden-import greenlet._greenlet`,
  - добавлены `--collect-submodules greenlet` и `--collect-binaries greenlet`.
- Цель: гарантированно подтянуть бинарный модуль `greenlet._greenlet` в `export_delivery_statuses.exe`.

## 2026-03-28-web.57
- Кнопка `Скачать Excel` переведена на выгрузку готового аналитического отчета (аналогично PDF), а не исходного сырого файла.
- В отчет Excel добавлены листы:
  - `Сводка` (ключевые KPI),
  - `Заказы` (полный список с этапами и причинами),
  - `Проблемные` (заказы с опозданием > 60 мин).
- Добавлена сортировка заказов в веб-интерфейсе:
  - `ресторан A→Я`,
  - `ресторан Я→A`,
  - `дольше всего`,
  - `сильнее опоздали к плану`.
- Выбранная сортировка применяется одновременно:
  - в таблицах дашборда,
  - в PDF-отчете,
  - в Excel-отчете.

## 2026-03-28-web.58
- Добавлены итоговые значения по ресторанам:
  - новый блок `Итоги по ресторанам` в веб-дашборде,
  - отдельный лист `Итоги по ресторанам` в Excel-отчете,
  - отдельная секция итогов по ресторанам в PDF-отчете.
- Для каждого ресторана считаются:
  - число заказов,
  - число и доля опозданий,
  - среднее и P90 общего времени,
  - средние времена этапов (обработка/готовка/сборка/доставка-выдача).
- Перепакован Windows-runtime:
  - worker запускается в режиме `--worker` внутри `saby_export_console.exe` (тот же бинарь),
  - это убирает зависимость от отдельного `export_delivery_statuses.exe` и снижает риск ошибок импорта `greenlet`.

## 2026-03-28-web.59
- Исправлен запуск на Windows при ошибке `HAR файл не найден`:
  - в `webui_server.py` добавлен авто-поиск HAR перед стартом задачи,
  - проверяются папки: текущая, папка приложения, `Desktop`, `Downloads`, домашняя.
- Если HAR найден, backend автоматически передает `--har <path>` в worker и пишет это в лог (`HAR auto-detected`).
- Цель: убрать зависимость от hardcoded macOS-путей `/Users/...` в Windows-сборке.

## 2026-03-28-web.60
- В веб-форму запуска добавлено поле `HAR (опционально)`:
  - можно явно указать путь к HAR-файлу на Windows,
  - путь сохраняется в браузере и подставляется при следующем запуске.
- Backend принимает `har_path` в `POST /api/run` и передает его в worker как `--har`.
- Расширен авто-поиск HAR:
  - помимо фиксированных имен теперь ищутся любые `*.har` в папках запуска, Desktop и Downloads,
  - добавлен приоритет delivery-файлов (`dost`/`delivery`) и `rest.saby.ru`.

## 2026-03-30-web.75
- Дополнительная стабилизация runtime-режима на Windows:
  - из `capture_runtime_service_meta` убран `page.reload`,
  - захват runtime-контекста теперь выполняется через первичную навигацию + активацию вкладки `Выполнен`.
- Цель: исключить сценарий самопроизвольного закрытия/краша Chromium сразу после авторизации.

## 2026-03-30-web.74
- Исправлен критичный сбой runtime-capture на Windows:
  - `Page.reload: Timeout ...` больше не валит выполнение,
  - `goto/reload` в `capture_runtime_service_meta` переведены в timeout-safe режим.
- Добавлен hard-fallback:
  - если runtime-capture падает (включая повторный), скрипт продолжает работу с универсальным runtime-шаблоном, а не завершается с code 1.

## 2026-03-30-web.73
- Исправлен кейс `Найдено заказов: 0` в режиме без HAR:
  - при `Runtime Done-запрос не пойман` перенос runtime-контекста теперь консервативный (без узких `Sales/SalesPoints/Workplaces/Warehouses`),
  - если первая выборка вернула 0, автоматически запускаются retry-варианты с ослаблением фильтров и выбирается лучший результат.
- Убран лишний шум в runtime-only запуске:
  - предупреждение `HAR файл не найден...` больше не печатается, если HAR явно не задан пользователем.

## 2026-03-29-web.72
- Web-консоль переведена в режим **без HAR**:
  - удалено поле HAR из интерфейса,
  - отключены передача и автопоиск HAR в `webui_server`,
  - запуск всегда идет через runtime-перехват в рабочей сессии Saby.
- Повышена стабильность single-EXE:
  - ошибка worker больше не роняет весь процесс `saby_export_console.exe`,
  - traceback печатается в лог, а UI остается доступным с корректным статусом ошибки.

## 2026-03-28-web.61
- Исправлена критичная ошибка Windows worker-режима:
  - при запуске `saby_export_console.exe --worker ... --har <path>` аргумент `--har` больше не теряется,
  - путь к HAR теперь корректно передается во встроенный `export_delivery_statuses`.
- В результате ручной путь HAR из формы и авто-обнаруженный HAR реально используются в выгрузке, а не игнорируются.

## 2026-03-28-web.62
- Убран жесткий блокер по HAR для Windows-сборки:
  - если HAR отсутствует или не подходит, скрипт автоматически переключается в runtime-режим,
  - SaleOrder.List берется из живого runtime-контекста страницы доставки.
- Добавлен встроенный fallback-шаблон истории:
  - если в HAR нет `История.History_Of_Instance`, используется встроенный шаблон,
  - это позволяет выгружать статусы без ручного подбора второго HAR-файла.
- Цель релиза: сценарий «скачал архив → запустил → авторизовался → работает» без дополнительных файлов.

## 2026-03-28-web.63
- Исправлен сбой Windows-сборки `Executable doesn't exist ... playwright ... chrome.exe`:
  - в CI теперь выполняется `python -m playwright install chromium`,
  - включен `PLAYWRIGHT_BROWSERS_PATH=0`, чтобы Chromium попадал в сборку.
- Добавлен runtime fallback браузера:
  - если встроенный Playwright Chromium недоступен, скрипт автоматически пытается запуститься через системный Chrome/Edge.
- Цель: полностью убрать требование ручного `playwright install` у конечного пользователя.

## 2026-03-29-web.64
- Усилен полностью автоматический режим без HAR:
  - если runtime `SaleOrder.List` не удается перехватить, больше нет аварийного завершения,
  - используется встроенный универсальный шаблон `SaleOrder.List`,
  - для истории остается встроенный fallback-шаблон `История.History_Of_Instance`.
- В лог добавлено явное сообщение:
  - `Использую встроенный универсальный шаблон SaleOrder.List`,
  - чтобы в веб-консоли было понятно, что запуск идет в fallback-режиме.

## 2026-03-29-web.65
- Добавлен автоматический smoke-check в CI для Windows-сборки:
  - проверка запуска `saby_export_console.exe --help`,
  - проверка наличия `chrome.exe` внутри `_internal/playwright/.../.local-browsers`.
- Это гарантирует, что релиз не публикуется без встроенного браузера Playwright и без рабочего EXE.

## 2026-03-29-web.66
- Исправлен аварийный сбой на первом history-запросе (`RPC HTTP error 404 ... History_Of_Instance`):
  - добавлен автоподбор совместимого варианта history-метода (header/body),
  - рабочий вариант сохраняется и используется дальше в текущем запуске.
- Добавлен безопасный fallback:
  - если history конкретного заказа не загрузился, запуск больше не падает целиком,
  - заказ пропускается с предупреждением в лог и обработка продолжается.

## 2026-03-29-web.67
- Исправлен сценарий `SaleOrder.List/3 не найден` при отсутствии runtime-шаблона:
  - в `wait_until_service_ready` ошибка метода больше не маскируется под «нужна авторизация»,
  - добавлено автовосстановление: повторный захват runtime-параметров и повтор запуска.
- При отсутствии runtime `ProductStateId=Done` автоматически включается эвристический fallback,
  чтобы запуск продолжался без ручного флага `--allow-heuristic-fallback`.

## 2026-03-29-web.68
- Расширен runtime-детектор метода заказов:
  - вместо жесткой привязки к `SaleOrder.List` теперь поддерживается любой метод `SaleOrder.*`,
  - это покрывает контуры, где список заказов отдается через другую сигнатуру/вариант API.
- Исправлена классификация ошибки метода:
  - `... не найден ...` теперь корректно обрабатывается для всех `SaleOrder.*`, а не только `SaleOrder.List`.

## 2026-03-29-web.69
- Исправлен сбой `TargetClosedError: Page.reload` в runtime-capture:
  - добавлено автовосстановление страницы при закрытии `page/context` во время `goto/reload/wait`,
  - capture runtime теперь повторяет попытку на новой странице вместо падения.
- Исправлен возврат пустого runtime-capture:
  - при отсутствии захваченных запросов функция возвращает корректный 5-элементный tuple с `runtime_sale_is_done=False`.

## 2026-03-29-web.70
- Исправлен ложный выбор runtime-метода `SaleOrder.CountersByFields`:
  - runtime-capture теперь принимает только list-подобные методы `SaleOrder.*` (по имени и структуре payload),
  - методы счетчиков больше не используются для реестра заказов.
- Добавлена обработка cursor-сигнатурной ошибки:
  - ошибка `разрешена курсорная навигация только по полям ...` теперь классифицируется как ошибка метода/сигнатуры,
  - автоматически запускается процедура автовосстановления с повторным захватом runtime-параметров.

## 2026-03-29-web.71
- Улучшен авто-выбор HAR в `webui_server`:
  - если в поле HAR указана папка, выполняется поиск подходящего HAR внутри неё,
  - при авто-поиске выбор файла теперь контентный (предпочтение HAR с `SaleOrder.List` и `Istoriya.History_Of_Instance`),
  - снижена вероятность выбора случайного HAR со счетчиками/нерелевантным трафиком.
